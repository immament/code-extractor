{
  "name": "code-extractor-search",
  "version": "0.0.1",
  "description": "Code Extractor",
  "main": "build/src/index.js",
  "author": "immament",
  "license": "MIT",
  "files": [
    "build/lib"
  ],
  "scripts": {
    "lint": "eslint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "eslint --fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "test": "jest --coverage",
    "test:watch": "yarn jest --watch",
    "posttest": "yarn run lint",
    "start": "node .",
    "prepublishOnly": "pinst --disable",
    "postpublish": "pinst --enable",
    "depcruise": "depcruise",
    "dep": "depcruise   --include-only '^(src/lib/modules/search)' --exclude '(__tests__|__mocks__)' --ts-pre-compilation-deps --output-type dot src/lib/modules/search | dot -T png > dependencygraph.png"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "loglevel": "^1.7.1",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.11.2",
    "dependency-cruiser": "^9.23.2",
    "gts": "^3.1.0",
    "jest": "^26.6.3",
    "jest-mock-extended": "^1.0.10",
    "pinst": "^2.1.4",
    "ts-jest": "^26.5.0"
  },
  "engines": {
    "node": ">=10.6.0"
  }
}
