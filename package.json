{
  "name": "code-extractor",
  "version": "0.0.1",
  "description": "Code Extractor",
  "author": "immament",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "test": "yarn jest",
    "test:watch": "yarn jest --watch",
    "search": "yarn workspace @imm/code-extractor-search",
    "ts": "yarn workspace @imm/ts-common",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "husky": "^5.1.1"
  }
}
